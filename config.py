USE_COLLOUT_FORMAT = True # Use callout format or not

# USER_ID is a unique identifier for the user. It should be a 10-character number.
USER_ID = YOU_USER_ID

# BOT_TOKEN is the authentication token for the bot, used to send messages and interact with Telegram's API.
BOT_TOKEN = "YOU_BOT_TOKEN"

# DAILY_FOLDER_PATH is the path to the folder where daily notes will be saved.
DAILY_FOLDER_PATH = "YOU_PATH_TO_DAILY_NOTES_FOLDER"

# LAST_UPDATE_ID_FILE_PATH is the path to the file where the last update ID is stored, used to track new updates from the bot dialog.
LAST_UPDATE_ID_FILE_PATH = "last_update_id.json"